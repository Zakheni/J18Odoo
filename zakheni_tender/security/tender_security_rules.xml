<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <data noupdate="1">
        <!-- Record rule: Tender Users see only their own tenders or team tenders.
             Tender Managers (and superusers) see all tenders. -->
        <record id="tender_tender_personal_rule" model="ir.rule">
            <field name="name">Personal Tenders</field>
            <field name="model_id" ref="model_tender_tender"/>
            <field name="groups" eval="[(4, ref('zakheni_tender.group_tender_user'))]"/>
            <field name="domain_force">['|', ('user_id', '=', user.id), ('team_ids', 'in', [user.id])]</field>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="True"/>
            <field name="perm_create" eval="True"/>
            <field name="perm_unlink" eval="False"/>
        </record>
    </data>
</odoo>
