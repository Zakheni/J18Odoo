<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="knowledge_action_documents" model="ir.actions.act_window">
        <field name="name">Documents</field>
        <field name="path">knowledge-documents</field>
        <field name="res_model">ir.attachment</field>
        <field name="view_mode">kanban,list,form</field>
        <field name="context">{'search_default_user_documents': True, 'search_default_group_folder': True}</field>
    </record>

    <!-- Search view: add folder field and grouping -->
    <record id="ir_attachment_view_user_documents" model="ir.ui.view">
        <field name="name">Documents search view: additional filters</field>
        <field name="model">ir.attachment</field>
        <field name="inherit_id" ref="base.view_attachment_search" />
        <field name="arch" type="xml">
            <search>
                <field name="res_model" />
                <field name="index_content" />
                <field name="folder_id" />
                <filter
                    name="user_documents"
                    string="Documents"
                    domain="[('res_model', 'not like', 'ir.%'), ('res_model', '!=', False)]"
                />
                <group string="Group by">
                    <filter
                        name="group_model"
                        string="Model"
                        context="{'group_by': 'res_model'}"
                    />
                    <filter
                        name="group_folder"
                        string="Folder"
                        context="{'group_by': 'folder_id'}"
                    />
                </group>
                <searchpanel>
                    <field
                        name="folder_id"
                        icon="fa-folder"
                        string="Folders"
                        enable_counters="1"
                    />
                </searchpanel>
            </search>
        </field>
    </record>

    <!-- Form view: add folder_id field -->
    <record id="ir_attachment_view_form_folder" model="ir.ui.view">
        <field name="name">Documents form view: folder field</field>
        <field name="model">ir.attachment</field>
        <field name="inherit_id" ref="base.view_attachment_form" />
        <field name="arch" type="xml">
            <field name="res_model" position="after">
                <field name="folder_id"
                    options="{'no_create_edit': True}"
                    domain="[('type', '=', 'category')]"
                    context="{'default_type': 'category'}"
                />
            </field>
        </field>
    </record>

    <!-- Tree view: add folder_id column -->
    <record id="ir_attachment_view_tree_folder" model="ir.ui.view">
        <field name="name">Documents tree view: folder column</field>
        <field name="model">ir.attachment</field>
        <field name="inherit_id" ref="base.view_attachment_tree" />
        <field name="arch" type="xml">
            <field name="res_model" position="after">
                <field name="folder_id" optional="show" />
            </field>
        </field>
    </record>

    <!-- Kanban view: show folder badge -->
    <record id="ir_attachment_view_kanban_folder" model="ir.ui.view">
        <field name="name">Documents kanban view: folder info</field>
        <field name="model">ir.attachment</field>
        <field name="inherit_id" ref="mail.view_document_file_kanban" />
        <field name="arch" type="xml">
            <xpath expr="//div[hasclass('o_kanban_card_header')]" position="inside">
                <field name="folder_id" />
            </xpath>
            <xpath expr="//field[@name='name']" position="after">
                <div class="text-muted small" t-if="record.folder_id.raw_value">
                    <t t-esc="record.folder_id.raw_value" />
                </div>
            </xpath>
        </field>
    </record>

    <!-- Top menu item -->
    <menuitem
        id="menu_document_root"
        name="Knowledge"
        groups="document_knowledge.group_document_user"
        web_icon="document_knowledge,static/description/icon.png"
        sequence="116"
    />
    <menuitem
        id="menu_document_section"
        name="Documents"
        groups="document_knowledge.group_ir_attachment_user"
        parent="menu_document_root"
        sequence="150"
    />
    <menuitem
        id="menu_document"
        name="Documents"
        action="knowledge_action_documents"
        parent="menu_document_section"
        sequence="0"
    />
</odoo>
